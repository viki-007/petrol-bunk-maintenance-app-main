name: petrol_bunk_maintainence
description: A new Flutter project.
publish_to: "none" 

version: 1.0.0+1

environment:
  sdk: ">=2.19.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  cloud_firestore: ^4.4.5
  get: ^4.6.5
  firebase_core: ^2.5.0
  firebase_auth: ^4.2.6
  google_fonts: ^4.0.3
  persistent_bottom_nav_bar: ^5.0.2
  local_auth: ^2.1.6
  image_picker: ^0.8.7+1
  image_cropper: ^3.0.2
  google_mlkit_text_recognition: ^0.5.0
  flutter_barcode_scanner: ^2.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

 
  flutter_lints: ^2.0.0

flutter:
  assets:  
    - assets/

  uses-material-design: true